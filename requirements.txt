# Offline core runtime — everything `neuroad demo` / `neuroad reproduce-finding`
# needs. Kept in sync with the [project].dependencies in pyproject.toml.
#
# `anthropic` is intentionally NOT listed here: it lives in the optional
# [claude] extra (see pyproject.toml [project.optional-dependencies]). The
# referee runs fully offline with Claude calls degrading to deterministic
# templates; install the live adjudicator with:  pip install -e ".[claude]"
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11
statsmodels>=0.14
matplotlib>=3.8
pyyaml>=6.0
jinja2>=3.1
requests>=2.31
